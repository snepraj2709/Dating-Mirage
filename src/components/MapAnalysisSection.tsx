import { ContentBand } from '@/components/ui/section';

export function MapAnalysisSection() {
  return (
    <ContentBand className="grid min-h-screen content-start pt-[72px]" id="map-analysis">
      <h2 className="mb-0 text-[clamp(1.75rem,4vw,3rem)] leading-[1.05] tracking-normal text-foreground">
        Map Analysis
      </h2>
    </ContentBand>
  );
}
